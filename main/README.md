# RSS 自动化处理系统

自动抓取 RSS 源、生成 AI 摘要并发布到 Jekyll 博客的完整解决方案。

## 📁 文件说明

```
main/
├── index.js              # 主程序（一键运行所有功能）
├── ai-summary.js         # AI 摘要生成模块
├── rss.json              # RSS 源配置文件
├── rss.json.example      # 配置文件示例
└── README.md             # 本文档
```

## 🚀 快速开始

### 1. 设置 API Key

```bash
export SILICONFLOW_API_KEY=你的硅基流动API密钥
```

或编辑 `ai-summary.js` 第 6 行直接设置。

### 2. 配置 RSS 源

编辑 `rss.json`：

```json
{
  "Nature": ["http://feeds.nature.com/nature/rss/current"],
  "Science": ["https://www.science.org/rss/news_current.xml"]
}
```

### 3. 运行

```bash
npm start
```

或

```bash
npm test
```

## ✨ 功能特性

- ✅ 自动抓取多个 RSS 源
- ✅ 保存为结构化 JSON（`rss/{分类}.json`）
- ✅ 智能去重（检查文章链接）
- ✅ AI 自动生成摘要
- ✅ 按日期和分类合并文章到一个 Markdown 文件
- ✅ Jekyll 博客兼容格式

## 📝 输出格式

### 文件命名

`YYYY-MM-DD-category.md`

例如：
- `2025-11-05-nature.md` - 包含该天所有 Nature 文章
- `2025-11-05-science.md` - 包含该天所有 Science 文章

### 文件内容

```markdown
---
layout: post
title: Nature Articles - 2025-11-05
date: 2025-11-05 14:30
category: news
author: sukiElaina
tags: [nature]
summary: AI summary of Nature articles
---

## [文章标题1](文章链接1)

AI 生成的摘要内容...

---

## [文章标题2](文章链接2)

AI 生成的摘要内容...

---
```

## 🔄 工作流程

1. **读取配置** - 从 `rss.json` 读取 RSS 源列表
2. **抓取 RSS** - 获取最新文章并保存到 `rss/*.json`
3. **按日期分组** - 将文章按发布日期和分类组织
4. **检查去重** - 检查 `_news/` 目录，过滤已存在的文章
5. **AI 生成摘要** - 对新文章调用 AI 生成摘要
6. **保存文件** - 追加到对应的 Markdown 文件

## ⚙️ 高级配置

### 修改 AI 模型

编辑 `ai-summary.js`：

```javascript
const MODEL_NAME = 'THUDM/glm-4-9b-chat';  // 可更换其他模型
```

### 修改请求延迟

编辑 `index.js` 第 197 行：

```javascript
await new Promise(resolve => setTimeout(resolve, 2000)); // 毫秒
```

### 自定义提示词

编辑 `ai-summary.js` 的 `summarizeArticle()` 函数中的 `prompt` 变量。

## 📊 运行示例

```
============================================================
RSS 自动化处理系统启动
============================================================

1. 读取 RSS 配置...
   找到 3 个分类

2. 抓取 RSS 源...

分类: Nature
  抓取: http://feeds.nature.com/nature/rss/current
  ✓ 已保存: Nature.json (75 篇文章)

3. 处理文章并生成 AI 总结...

处理分类: Nature
  共 75 篇文章

  处理日期: 2025-11-05 (15 篇文章)
    发现 2 篇新文章
    ✎ 处理: Article Title 1
      调用 AI 生成总结...
      ✓ 已生成总结
    ✓ 追加到现有文件: 2025-11-05-nature.md

============================================================
✓ 所有任务完成！
============================================================
```

## 🔧 故障排除

### API Key 错误

```bash
export SILICONFLOW_API_KEY=sk-your-actual-key
```

### RSS 抓取失败

检查网络连接和 RSS URL 是否有效。

### 文件权限错误

确保对 `rss/` 和 `_news/` 目录有写权限。

## 📅 定时任务

设置每日自动运行：

```bash
# 编辑 crontab
crontab -e

# 添加定时任务（每天早上 8 点）
0 8 * * * cd /workspaces/sukiElaina.github.io && npm start
```

## 📄 许可证

ISC

---

**提示**：详细文档请查看 `README-AUTO.md`
